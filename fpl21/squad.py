class Squad():

    def __init__(self, team):
        self.team = team # [[gks], [defs], [mids], [att]]
        
    def transfer(in_id, out_id):
        if position(in_id) != position(out_id):
            raise ValueError("Position of transfer player doesn't match")
        else:
            self.team[]

